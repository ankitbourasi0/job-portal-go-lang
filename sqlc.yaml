version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries" #pick queries from here and generate in internal/database
    schema: "sql/schema"
    gen:
      go:
        package: "database"
        out: "internal/database" #code generate here
        sql_package: "pgx/v5"